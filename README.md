# 千千结 · TauriTavern 适配试用版

面向 **iOS / TauriTavern 2.2.0** 的个人适配，基于 [atonal519/ST-MyriadKnots](https://github.com/atonal519/ST-MyriadKnots)。原插件与界面归原作者，本分叉增加 TT 存储适配及请求等待、阶段显示修复。**已完成代码与自动化测试，尚未完成 iPhone 真机验收。**

## 在 iPhone 安装

当前版本：**千千结 0.5.1 + TT 适配 tt.6**。修复批量补齐在摘要已保存后仍显示“正在处理摘要”的问题，分别显示正在分析人物状态或同步记忆状态；任务结束后清除打开面板时留下的“正在处理”提示。已有摘要不需要重做。

包含 tt.5 的独立外观开关：在「设置 → 通用设置 → 外观」使用「显示楼层召回卡片」和「显示楼层记忆卡片」，分别控制“本轮召回”和“第几个结”。默认均开启，对所有聊天立即生效并保存选择；隐藏整块卡片，不影响后台摘要、人物分析和召回注入。

包含 tt.4 的等待状态修复：摘要保存后会显示“摘要已保存，正在分析人物状态”，已保存楼显示可用；人物分析结束前仍保留任务占用。修复宿主请求不响应取消时无法退出等待、流式接口发出结束标记后仍等待断开的问题。人物分析保留原版最多 3 次尝试规则，180 秒是每次请求的超时，并非整项任务总时限；无需为了这项修复开启流式。

包含 tt.3 的原生 JSON 字段顺序及旧索引兼容修复。已安装本仓库旧版的用户，可在 TT 扩展管理中更新本扩展后重启应用，无需卸载重装。升级前请先导出 TT 完整数据备份；旧版 TT 记忆仍保存在原位置，更新不会自动重做旧摘要。若此前“补齐缺失”报后端数据处理失败，更新后先“刷新状态”，再“补齐缺失”。新版“千事”的旧楼内容可按需手动补齐。

1. 如果已经安装原版千千结，先禁用原版，避免两份插件同时运行。
2. 打开 TT → 扩展 → 安装扩展，粘贴以下仓库地址：

   `https://github.com/yui-ovo/ST-MyriadKnots-TT`

3. 安装完成后重启 TT，确认扩展列表显示「千千结 · TT 适配试用版」。
4. 打开一个测试用的单人聊天，在千千结设置中配置分析 API 和摘要 API，再启用插件。
5. 先测试几轮对话、摘要和召回，然后完全退出并重新打开 TT，确认记忆仍然存在，再用于长档。

**TT 不需要安装 ST-BaiNiaoData 服务端插件，不需要运行 npm，也不需要修改 config.yaml 或重装 TT。** 模型分析/摘要仍然需要你自己的模型 API。

记忆保存在 TT 的数据目录中，备份时请使用 TT 的完整数据导出。已有 SillyTavern / Luker 后端记忆不会自动迁移过来。回退时禁用本适配版即可；不要同时启用原版和适配版，也不要把「完全重构」当作安装修复。

详细实现、验证结果与限制见 [TT 适配说明](docs/tauritavern.md)。下面保留原作者的功能说明；涉及服务端安装的段落只适用于 SillyTavern / Luker。

---

# Myriad Knots（千千结）

`ST-MyriadKnots` 是 SillyTavern 的长期记忆扩展，界面中文名为“千千结”。项目仓库：[github.com/atonal519/ST-MyriadKnots](https://github.com/atonal519/ST-MyriadKnots)。

## 当前功能

- 为每个聊天建立稳定身份，并保持聊天、角色卡与 Persona 之间的数据隔离。
- 从同一角色聊天创建分支时，新聊天会继承实际保留楼对应的摘要与人物状态，并沿用这些人物的最新版资料；源聊天后续楼不会带入，复制过程不调用模型。
- 从稳定 AI 楼提取带证据的 FloorMemory；支持历史重建、断点继续、逐楼重算和用户修订摘要。
- 手动历史补齐与完全重构在首楼建立人物状态基础后，后一楼摘要可与前一楼 CSE 同时请求，每路最多一个请求。摘要失败会继续其他楼，CSE 不越过摘要缺口；CSE 失败只暂停本批人物状态分析，其他摘要继续，已保存结果保留。
- 完全重构会删除当前聊天全部千千结记录及前情，包括人物资料、头像、重要人物选择、时间事项、人工修改和旧版本，再以新身份从聊天正文重新建档；普通“删除当前聊天记忆”仍保留前情。两者都保留聊天正文、其他插件数据和全局设置。
- 已保存楼的正文后来发生普通编辑时，用户仍可基于当前正文修订摘要、时间、地点和人物；保存前后若目标再次变化则拒绝迟到写入。
- 摘要时间优先读取标准 QQJ/SDC/myknots 时间戳；缺少标准时间戳时，可从正文原文中的成对标签补充完整时间参考。补充标签默认留空，可在设置中显式填写 `Ti` 或其他自定义标签。该设置只影响后续摘要提取，不改变正文清洗，也不会自动重做旧摘要。
- 以正文为最高事实源，通过 CSE 维护人物核心、适应性与情境状态；坏条目不会误清空旧分类，合法证据也不会被同批坏引用连坐。
- 可选时间推演默认关闭，直接读取当前选中、清洗后的 AI 正文，使用摘要 API 追踪身体状态、周期与约定期限，摘要与 CSE 可为空。历史观察满足时间条件时可同次登记并推算到当前，未知或倒退时间不强算；推测独立保存，不改写旧 CSE，也不是已发生的新事实。
- “千结 → 近期事项”可折叠查看、编辑和整理当前追踪清单；“补查历史”先显示待查楼数、预计批次和 API 调用量，确认后分批读取正文。楼内“本轮时间参考”展示该楼原回执真正入选的参考，追踪总数不等于本轮注入数量。
- 千事随新楼摘要在同一次请求中提取事件、事项进展和明确先后关系，并参与普通召回；旧摘要不会自动补写千事。主面板“千事”页展示完整已保存年表与事项经过，并可在明确确认后手动补齐旧楼；千事没有独立开关。
- 原生批量删除聊天末尾后，摘要与 CSE 可回退到保留楼，千人基础资料保持最新版；既成卡档可在记忆管理点击“刷新状态”，经安全完整读回核验后恢复，普通打开不自动恢复。
- 在正文生成前根据当前 user 输入、近期上下文与历史事实排序召回；普通召回和前情使用 D2，故事时间仍使用 D0。同一条 user 消息的续写、重生和 swipe 会复用首次召回，不被后台晚到变化改写；局部 CSE 记录损坏时，仍可只读使用通过核心图校验的摘要，新学别名也进入统一人物目录。
- 人物资料批量整理会保存能唯一绑定的合法人物，并在原结果位置说明遗漏、冲突或未知目标。
- 主动人物重新整理不参考旧 AI 档案并替换旧 AI 字段，保留人工字段、头像、排序与重要人物选择；自动更新继续按新材料增量整理。
- 设置页提供总开关、分析／摘要／召回 API、世界书整本排除、正文清洗标签、摘要／人物资料／人物状态提示词、已记忆旧楼自动隐藏、当前聊天记忆删除和外观设置。摘要默认跟随分析，召回默认跟随摘要，也可独立选择共享预设；不同预设仍可能共用同一账号的并发额度。
- 世界书整本排除在读取前生效；异步字体结果仅在对应 URL 仍为当前选择时应用。

主面板分为“千人”“千结”“千事”“双丝网”和“设置”五页：分别查看人物资料、逐楼记忆、剧情年表、人物状态以及插件配置。后台记忆和召回不依赖面板保持打开。

同一 SillyTavern 页面里的其他扩展可通过版本化全局桥只读获取当前聊天的记忆文本、最近一次正文生成准备的轻量材料或结构化快照。调用方式与字段合同见 [公开读取 API](docs/public-api.md)。

常见问题可先看应用内“设置 → 教程与配置文件 → 教程文档”；完整排查步骤见[排障手册](docs/troubleshooting.md)。

## 0.5.0

- 默认正文故事时间提示要求起止时间戳都带故事年份或纪年，并优先沿用可靠依据；没有已有可靠纪年时，模型依据开场白与本轮生效世界书设定，或按世界观创作故事纪年，不使用系统现实年份。自定义提示词和构画的时间戳协调方式保持原样。
- 新增“千事”完整年表页：按可靠剧情时间分段，支持搜索、同日归组，并可查看事项的完整经过、来源楼和历史补齐进度。
- 千事事件与已有历史／人物候选共同参与一次召回选材；楼内会展示实际入选并注入的时间参考，年表项目多不代表每轮都会全部送入正文。
- 楼内“事”页可展开“本轮千事进度”，只显示本轮实际注入的千事内容。
- 已停止的时间事项支持批量永久删除其事项及时间历史，删除后无法恢复；此操作需要支持永久删除接口的新版白鳥后端。旧后端下此操作不可用，普通记忆、删除与恢复仍可使用。
- 优化暖缓存复用、自动扫描与批量读取的事件循环让步，减少部分重复读取和长任务阻塞。不能据此认定所有冷读超时或酒馆整页卡顿已解决。
- 更新后刷新酒馆即可使用；旧摘要不会因插件更新而自动重跑。
- 千事“重要”自动判断与手动标记本版暂时撤除；旧事件中的标记仍可读取并保留，但不再参与展示。“重要人物”功能不变，千事重要性后续另议。
- 千事与刻度关联到同一事项时，重叠的当前提醒只展示一次；历史时间线仍保留。历史补齐兼容带 JSON 围栏的 `textData` 和旧 `responseText` 输出。

## 0.4.3

- 同一事件中同一人物使用本名、别名等不同称呼时，不再因重复参与边阻断摘要准备；已有记录可直接兼容，无需重做已存摘要。

## 0.4.2

- “复制状态诊断”会补充自动摘要失败的具体准备步骤、独立错误类型、安全错误码、固定技术说明和插件内代码位置；仍不会包含错误原文、完整堆栈、聊天正文、内部 ID、API 地址或密钥。
- 同一千事关系被跨楼重复记录时只投影一次，避免高楼摘要在准备千事候选时因重复图边而停止。

## 0.4.1

- “复制状态诊断”会补充安全的错误类型、失败阶段和累计次数，帮助定位自动摘要失败；仍不会包含错误原文、堆栈、聊天正文、API 地址或密钥。

## 0.4.0

- 新增存储管理：可查看当前记录占用并手动永久清理不再被当前记录引用的旧基础记录；它不处理回收站，自动周期清理默认关闭，开启后才会运行。永久清理需要已发布的新版白鳥后端；未升级后端时普通记忆、删除与恢复仍可继续使用。设置页重新整理了管理区布局，普通删除与恢复仍沿用回收站语义。
- 摘要与人物状态支持关键词搜索和详情查看；人物状态时间线明确区分新增、更新与移除。人物状态完全重构的失败楼号、进度和错误会持续保留在管理页，刷新状态不会把旧失败提示误写成当前读取失败。
- 检测到连续 AI 回复时，可在记忆页确认后分别登记并按顺序摘要。摘要时间兼容正文里的多段时间区间；旧记录只有在当前可靠正文时间回退可用时才显示完整区间。面板标题与人物名不再被固定宋体覆盖，自定义字体设置会作用到这些位置。
- 优化新鲜读取、人工刷新、同 root 复用、人物状态续跑和逐楼读取：健康缓存日常打开不再重复读取整图，root 变化时仍会重新核验；已经提交的人物状态进度会从当前可达记录继续。
- 普通召回与前情的注入深度统一为 D2，故事时间保持 D0。同一条 user 消息首次生成的完整召回会冻结复用，续写、重生与 swipe 不再重复读取来源或重跑选材；删除该 user 楼后创建的新消息仍会重新召回。
- 千事随摘要同次生成，以每楼记忆为唯一权威，保存事件、事项接续和明确先后关系；当前有效楼会派生剧情图并把相关进度纳入普通召回。千事没有独立开关；当时尚无专属时间线前端。旧摘要和旧人物状态不会自动重写。
- 千事输出合同明确使用 `event-N` 事件局部编号及 `candidate-N` 旧事项候选编号，事件关系不再用标题代替编号；单个千事条目有误仍不会触发整次摘要重试或丢失合法摘要。
- 刻度长材料继续使用约 60000 估算 token 的保守输入预算；“千人”多人整档继续使用 60000 字符输入门槛，两者不是同一单位。

## 0.3.1

- 电脑端主面板滚动条改为更窄的半透明圆角样式，并移除上下箭头；深浅主题下都会在悬停时稍微加深。
- 移除主面板的蓝灰色外缘阴影，极暗背景下不再出现大范围晕光。

## 0.3.0

- 历史补齐和完全重构可选择“高楼压缩模式”：每 10 个连续 AI 楼合成一份范围记忆，并在批末分析一次人物状态，能减少请求与记忆数量，但会主动舍去更多枝节。需要逐楼细节时继续选择普通模式，每楼独立保存与分析。
- 压缩记忆保留实际成员楼来源；成员删除、换绑或来源对象变化时不会注入失效结果。高楼召回提高智能选材输出容量，并在同一 5 秒准备边界内复用经版本核对的快照；普通召回材料总上限提高到约 8000 token，仍按相关性取舍，不会为了填满额度硬塞材料。
- 召回取消旧的固定楼数与人物项数资格帽，聚合同线材料只受最终总预算约束；近期摘要、远期旧事、人物当前状态与历史变化统一去重。楼内召回卡支持更多剧情线，复选框与标题在窄宽度下不再互相挤压。
- “近期事项”增加批量完成、暂停和移除；已明确结束且无持续影响的事项可退出自动跟进，重伤、慢性或仍有影响的事项会继续保留。刻度长材料整理使用最高约 60000 估算 token 的保守输入预算，已保存观察和人工修改保持原有来源含义。
- “千人”可勾选多位人物后一次整档整理，单次输入最高 60000 字符；完整新档替换旧 AI 字段，同时保留头像、排序、重要人物选择与人工内容。成人偏好字段只整理独立、稳定的偏好与边界，剔除具体经历、场景举例和解释。

## 0.2.63

- 已有记忆楼带有当前聊天的精确有效楼标识时，普通正文编辑不再阻止首个尾部孤儿标识的安全恢复；原有顺序、位置、清洗配置、唯一性和跨聊天校验保持不变，已有楼及其摘要、人物状态和人物资料不会因此改写。
- 新分支首次继承失败并留下没有 root 的半成品后，若再次继承遇到精确记录冲突，会沿原源聊天改用新的独立目标完成复制。旧半成品不删除、不覆盖也不作为来源；已存在 root、读取异常、普通网络或匹配错误仍停止处理。

## 0.2.62

- 当已有记忆楼与当前正文构成完整且内容一致的前缀，而紧随其后的首个新楼残留了当前记录不存在的同聊天楼标识时，打开聊天或刷新记忆状态会清除该条孤儿标识并继续正常登记。正文、已有记忆、其他插件数据和不同楼标识保持不变；中段、跨聊天、重复、格式无效或内容不一致的冲突仍会停止并要求核对。该修复不代表已经确认孤儿标识的形成原因，也不放宽原有绑定校验。

## 0.2.61

- “待核对原因”和“复制状态诊断”会区分消息标识指向当前记录之外的楼，或多楼共用同一标识；诊断只包含原因、楼号与计数，不包含聊天正文。本次仅补充诊断信息，未放宽校验，也未宣称已修复分支问题根因。

## 0.2.6

- 时间事项某批只有部分结果有效时，成功项继续保存；同一正文不会因刷新或重载反复请求，之后出现新正文仍会正常更新，不再被旧的“部分完成”永久挡住。
- “近期事项”会在能够可靠对应到具体事项时标注本次失败原因，保留原内容，并可沿用现有入口编辑或移除。后续模型成功评估会清除标注；移除失败事项后，仍有其他失败或无法定位错误时继续显示部分完成。正文未检查范围独立保留，不会因移除事项被记作已覆盖。旧记录缺少事项 ID 时不按返回顺序猜测，无法可靠定位的错误仍保留批次提示。

## 0.2.5

- 修复时间事项已返回有效人物 ID 且姓名对应时，仍可能因其他同名记录被误判为人物归属不明确的问题。已有事项继续沿用原人物归属，只有姓名仍无法唯一归属或 ID 与姓名相悖时仍会拒绝；同批合法结果和此前已保存结果保持不变。更新并刷新页面后，可在“近期事项”中继续“补查历史”处理尚未完成的批次。

## 0.2.4

- 历史时间事项分批补查完成后，最多额外调用一次接口更新当前事项；同场景、相近时间且共同原因造成的事项可在同次请求中归并，人工修改后的事项仍参与后续更新。当前评估按 BM25 与等待轮次安排送审，未纳入或未有效回答会明确显示，但不保证模型逐项作答；自动淡出尚未加入。
- 日期读取兼容更多中文、全角、特殊纪年、闰月和星期写法；能确认的时间差继续计算，未知月长的跨月推进不猜。人物资料与用户设定中的生日、明确年度纪念日会增量提取，临近 7 日时参与现有时间参考选材。
- 时间参考使用最多 600 token 的子预算，普通召回总预算保持 4000 token。当前故事时间不再受摘要进度牵连；时间校正或提醒失效时只撤除对应内容，其余已选召回材料继续使用。
- 精校 CSE 当前情境与关系对象；楼内人物状态可正确展示自身状态。所有手动编辑保存栏移除整块底色与分隔线；摘要按钮不再跟随滚动，长表单保留跟随。
- 撤除旧召回状态推演的模型任务、额外 1000 token、注入、统计和专属展示；已有旧回执仍可只读核验，不会被新生成复用。

## 0.2.3

- 时间事项请求使用 S1、S2 等短来源编号，并兼容精确完整来源键，避免抄写长键出错。一项来源或字段无效时保留同批合法事项，明确提示坏项，未完成的正文仍待补查；部分完成后暂停，需手动继续，不自动反复请求。
- 模型返回后明确显示保存阶段；无新正文的补算部分完成时仍可手动继续，完整成功才记为已检查。人工修改、停止状态及此前合法结果保留。

## 0.2.2

- 时间推演统一按约 30000 估算 token 的完整输入预算分批，包含提示词、正文元数据及辅助材料；优先完整楼合批，每批最多 20 个 AI 楼，单楼自身超预算才按段落切片。移除旧小片和字符上限造成的过多调用，正文完整保留，失败后仍只补剩余范围；API 输出上限保持不变。

## 0.2.1

- 时间推演改为直接读取当前选中、清洗后的 AI 正文，使用摘要 API，不依赖摘要或 CSE 齐全，也不重做人工摘要。默认从开启时当前 AI 楼起追踪；补查历史先预估并确认，再按时间顺序分批处理，长楼分片，停止或失败后可继续剩余范围。
- 人物资料、人物状态和近期事项可通过各自菜单人工编辑；近期事项支持确认完成、暂停、移除及恢复追踪，保存不调用模型，也不改写旧楼回执。
- 召回准备复用已确认的记忆来源；提交和回执复用只核对实际选入的时间校正与提醒，无关时间推进不再误作废整轮召回，选中内容或来源改变仍拒绝旧注入。诊断保留各阶段进度与耗时，并支持复制召回回执。
- 精校“当前情境”：保留仍有效的处境、身体状态和关系反应，已经发生的经历细节交给摘要，合并同义重复；新分析使用新规则，旧档不自动重写。
- 补充前后端安装与插件排障说明，区分加载、记录读取和模型 API 失败，保留数据后按失败步骤排查。

## 0.2.0

- 新增默认关闭的时间推演，在“设置 → 记忆设置”开启，分析身体状态、周期与约定期限，心理和关系仍由 CSE 维护。启用后有材料或时间节点变化时可能额外调用分析 API，每批最多一次；与构画“刻度”重复注入时会提醒确认。
- 历史身体观察足够早时，可在首次整理的同一请求中保留原观察并推算当前状态；未知或倒退时间不硬算，推测不作为新事实。旧已登记但无推测的事项可明确手动整理补算，成功检查同材料不重复，失败可重试。
- “千结 → 近期事项”集中提供当前追踪的只读清单、整理与失败重试；读取失败可单独重试读取。楼内“本轮时间参考”保留该楼实际签名注入的独立参考和明确时间校正，不从最新清单补入预算舍弃项，也不回写旧 CSE。
- 手动历史补齐和完全重构在首楼建立人物状态基础后，下一楼摘要可与前一楼 CSE 并行，各路最多一个请求；CSE 不越过摘要缺口，失败不阻止其他摘要。召回 API 可独立选共享预设，默认跟随摘要；不同预设不保证不同账号额度。
- 完全重构清理当前聊天全部千千结记录及前情，包括人物资料、头像、重要人物选择、时间事项、人工修改和旧版本，再以新身份从正文重建；正文、其他插件数据和全局设置保留。普通删除保留前情，普通记录清理最多同时四路，根记录与身份绑定最后处理。
- 原生批量删尾可回退到保留楼的摘要与 CSE，千人基础资料保持最新版；既成卡档可在记忆管理“刷新状态”安全完整读回后恢复，普通打开不自动恢复，不泛化为任意中间删除。
- 主动人物重新整理不输入旧 AI 档案并替换旧 AI 字段，人工字段、头像、排序和重要人物选择保留；自动路径仍增量更新。首批失败保旧档，后批失败保本轮已成功保存部分。
- 近期事项支持正确折叠，同源读取复用缓存，同内容通知保留列表节点与滚动，状态和按钮仍实时更新。

## 0.1.28

- 摘要可从清洗前正文的成对标签读取完整时间参考；补充标签默认留空，可显式配置 `Ti` 或多个自定义标签，清空即关闭；标准 QQJ/SDC/myknots 时间戳仍优先。
- 本楼与最近前楼的时间参考会进入同一次摘要提取，不增加模型调用，也不改变正文清洗；已有旧摘要不会自动重做。

## 0.1.27

- 角色卡重命名后，同角色现有聊天及未打开聊天的千千结身份绑定会同步到新定位，后续继续使用原有记忆；这项保护从改名时开始生效，不会自动修复过去已经错绑的记录。
- 公开桥新增同步 `getPromptSnapshot()`，轻量读取当前聊天最近一次正文生成准备并注册过的普通召回与前情材料；它不读取后端、不调用模型，也不附带全量摘要、CSE 或人物档案结构。
- “测试连接”改用简短纯文本请求和通用语义解析，沿用默认输出上限，不再要求接口返回特定 JSON 字段，兼容只返回正常文本的预设。

## 0.1.26

- 正文清洗时，显式保留的内层标签内容不再随未保留的外层标签一并删除；内层未保留块仍会清除，原有字面包裹规则保持不变。

## 0.1.25

- 创建同角色聊天分支时，普通记忆记录最多同时写入 4 条；继承范围、完成核验和源聊天保护保持不变。
- 删除当前聊天记忆不再被独立的隐藏恢复步骤阻断；有效的千千结隐藏标记与楼层旧标识会在同一次保存中清理，相关楼层恢复显示，聊天正文保留。
- 新分支会把继承消息中的千千结隐藏标记换绑到新聊天，源聊天标记保持不变。
- 删除记忆形成空档后，可直接点击“完全重构”建立新档，并从保留的聊天正文重新生成记忆。

## 0.1.24

- 当前聊天还没有已加载记忆时，仍可删除该聊天的千千结记忆；聊天正文保留，之后可用“补齐缺失”从保留正文重新建档。
- 用户长期倾向总览会显示针对他人的条目及对象，同一条关系倾向仍保留在对应关系栏。

## 0.1.23

- 从同一角色聊天新建分支时，首次打开会把分支实际保留正文对应的摘要与人物状态复制到独立新档，并沿用这些人物的最新版资料；复制过程不调用模型。
- 已经建立为独立档案的旧分支不会自动补继承。较大的聊天首次复制需要逐条保存和核验，完成前请保持当前分支打开并耐心等待。

## 0.1.22

- 自动人物资料更新只合并本次新摘要，并在本地按人物和内容去重后整理一轮。
- 人物建档回复明确要求根对象包含 `profiles` JSON 数组，数组内逐项对应目标人物。
- 删除聊天记忆会同时清理消息外层和备选回复中的旧标识，已卡档可从原入口重新建档；若仍缺失，请确认正文保留标签正确，再删除当前聊天记忆、刷新酒馆并“补齐缺失”，正文会保留，摘要会重新生成。

## 0.1.20

- 将用户可见的技术错误改为清楚的中文说明，同时保留便于排查问题的细节。
- 清理界面残留的内部术语，统一设置、记忆管理和人物资料中的可见名称。
- 删除确认无用的旧代码与样式，保持现有存档和功能流程不变。

## 0.1.19

- 浏览器缺少 SHA-256 接口时改用 SillyTavern 现成实现，初始化与完全重构可正常计算记忆指纹。
- 没有打开聊天时可启用插件并保持待机；之后打开聊天会自动准备对应记忆。

## 0.1.18

- 修复浏览器 UUID 接口不可用时重新启用等流程报错，改用 SillyTavern 现成的编号接口。
- 连续人物变化中的相同依据只保留一份，同时保留状态识别文本、对象与隐私边界。

## 0.1.17

- 完善 CSE 合并、人工锁与状态语义，精校摘要和人物状态提示词；同时修正摘要证据与身份、人物资料来源、世界书过滤、前情引用和后端诊断。
- 设置界面与召回卡片更易读：旧事使用四列胶囊，人物当前状态和人物变化均可独立折叠。
- 普通召回材料预算保持 4000 token，总量仍受宿主上下文约束。
- 未选入统计会明确区分原因，并分别显示接口往返与本地选材耗时。

## 0.1.16

- 历史补齐会先处理摘要，再处理 CSE。某楼摘要最终失败时会跳过该楼并继续后续楼；只有 CSE 自身失败才会停止 CSE 阶段，已保存结果都会保留。
- 摘要提取与 CSE 分析各自最多发出 3 次请求；可恢复错误会沿用原错误类型重试，取消后不再继续。正文召回仍保留原来的一次重试。
- 同一浏览器会按聊天保留 CSE 逐楼失败和自动任务外层失败提示，刷新后仍可核对。
- 设置页最底部新增与通用设置同级的“教程与配置文件”抽屉；其中保留完整 8 节教程文档，并集中放置 API 说明、示例和复制功能。

## 0.1.15

- 召回模型发生 API 或网络等技术故障时，整轮选材会再尝试一次；第二次仍失败则可靠停止本次正文生成。正常完成但没有可靠命中的情况仍按空召回处理。
- 双丝网某个关系方向当前为空时，只读显示该方向最后一次已保存的历史关系和实际楼号，不改写当前人物状态。
- 版本化公开桥新增同步 `getSnapshot()`，以独立副本读取当前已加载的摘要、CSE 与千人档案；记忆管理页新增默认折叠的“API 接口”调用说明。

## 0.1.14

- 可在当前聊天的记忆管理页粘贴长篇前情；原文随聊天保存，生成正文时按相关性选段，并与普通记忆共享原有预算。
- CSE 分析和人物资料整理会参考相关前情；首次整理人物时参考范围较大，后续更新使用较小范围，人物长资料仍沿原流程分批处理。
- 提交记忆时会复用本次会话中已确认写入的内容，减少重复读取；若摘要已经生成但保存失败，在原正文与处理提示未变化时，手动继续会复用这次结果。

## 数据与兼容

千千结通过 ST-BaiNiaoData 的通用记录接口保存记忆数据，并使用 revision/CAS 避免迟到任务或并发写覆盖当前状态。切换聊天、切换 Persona 或关闭插件会使旧会话任务失效；总开关关闭后停止后台读取、AI 任务与召回注入。

同角色分支会获得独立的聊天身份和存储目录。分支初始化完成后，源聊天与分支可分别修改或删除；跨角色复制仍按独立新聊天处理。

删除当前聊天记忆会保留聊天正文和全局 API／提示词设置；后端记录进入回收站，当前不会自动永久清除。移动端面板使用浏览器原生纵向滚动；“详细诊断”可复制最近 24 条本地界面手势状态，不包含聊天正文或输入内容，也不会上传。该诊断用于继续定位偶发滚动问题，不代表所有手机壳浏览器均已完成真机验收。

## 安装

千千结前端与白鳥数据后端是两份插件，需分别安装：

1. 安装 [ST-BaiNiaoData 后端](https://github.com/atonal519/ST-BaiNiaoData)。Luker 可用后台的服务端插件管理；原生酒馆默认将后端放在根目录 `plugins/ST-BaiNiaoData`，确认其中直接有 `index.mjs`，不要多套一层文件夹，也不要装进前端的 `public/scripts/extensions/third-party`。
2. 检查实际运行的酒馆根目录 `config.yaml`：将已有的 `enableServerPlugins` 设为 `true`，不要追加重复键。默认 `serverPluginsPath: ./plugins`，若改过则按其路径安装；`enableServerPluginsAutoUpdate` 只控制自动更新。
3. 安装或改配置后完整重启酒馆服务，再刷新网页。仅刷新网页或切换千千结开关不会加载服务端插件。
4. 在酒馆第三方扩展安装入口粘贴本仓库 URL。面板入口位于魔法棒菜单，也可在设置中开启悬浮球；生产清单只加载 `dist/qqj-app.js`。打开实际聊天后配置模型 API，新聊天逐步记录，旧聊天按需要手动补齐。

遇到 404、持续读取中或记忆突然为空，先看 [排障手册](docs/troubleshooting.md)。模型 API 和记忆存储后端的错误要按失败地址区分；排查后端加载不需要删除记忆或完全重构。
